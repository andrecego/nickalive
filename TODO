caption on lightbox gallery
transition on hover


https://github.com/yfxie/activestorage-imgur/
this gem has built-in validation to validate image file. attachment can be nil, if it presents, it only accept image type. 
if your app only accept files with image types, it should be fine.

<% @photos.each_with_index do |photo, index| %>
  <%= link_to photo.picture, {'data-lightbox': "grafitti", 'data-title': photo.title, class: "image-box" } do %>
      <%= image_tag(photo.picture, {class: "thumb-image"}) %>
  <% end %>
<% end %>